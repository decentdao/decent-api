{
  "name": "decent-ponder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev --schema onchain --disable-ui",
    "start": "ponder start --schema onchain",
    "ponder:list": "ponder db list",
    "psql": "brew services restart postgresql@15",
    "lint": "eslint ./**/*.ts ./**/*.*.ts"
  },
  "dependencies": {
    "@fractal-framework/fractal-contracts": "^1.4.2",
    "hono": "^4.5.0",
    "ponder": "^0.10.16",
    "viem": "^2.21.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "eslint": "^8.53.0",
    "eslint-config-ponder": "^0.10.7",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.14"
  }
}
